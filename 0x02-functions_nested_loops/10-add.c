#include "main.h"
/**
 * add - it adds two integers.
 * @num1: it's the first integer to be added.
 * @num2: it's the second integer to be added.
 * Return: The result of the addition.
 */
int add(int num1, int num2)
{
return (num1 + num2);
}
