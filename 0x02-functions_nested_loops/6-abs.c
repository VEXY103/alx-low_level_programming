#include "main.h"
/**
 * _abs - it computes the absolute value of an integer.
 * @n: it's the integer to be computed.
 * Return: The absolute value of the integer.
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (-n);
}
}
